# VulnAgent V0.4 课程测试要求验收缺口与后续开发

> 编写日期：2026-09-11  
> 文档目的：如实说明“大模型、加壳闭源软件、混淆闭源软件”三项课程测试要求的当前完成度，并给出可直接执行的后续修复、开发与验收方案。  
> 结论口径：只有同时具备可复现的目标、漏洞发现结果、独立复核、有效证据链和受控利用验证，才记为完整通过；不能把模型 API 接入、壳/混淆识别、解壳、口令求解或自然语言推测等同于漏洞利用成功。

## 1. 课程测试要求

课程要求包含三组对象：

1. 支持 DeepSeek、智谱、Kimi 等任意 2 种开源大模型软件的漏洞挖掘和利用验证；
2. 支持任意 2 种具备加壳功能的闭源软件的漏洞挖掘和利用验证；
3. 支持任意 2 种具备混淆功能的闭源软件的漏洞挖掘和利用验证。

原文对“大模型软件”的角色存在两种可能解释：

- **解释 A：模型是漏洞分析引擎。** 即 VulnAgent 至少能够选择两种模型参与同一套漏洞发现和复核流程。
- **解释 B：模型软件本身是被审计目标。** 即选择至少两个开源大模型项目，对其源代码或二进制发行物完成漏洞发现和受控利用验证。

当前实现能够较好支持解释 A，但没有完成解释 B。正式答辩前应向教师确认口径；在未确认前，演示和文档不得声称解释 B 已完成。

## 2. 总体结论

截至 2026-09-11，**三项要求不能整体标记为全部完成**。

| 测试项 | 已完成部分 | 关键缺口 | 当前结论 |
|---|---|---|---|
| 任意 2 种开源大模型软件 | DeepSeek、GLM、Kimi 三个 Adapter；真实 API 调用；统一 `BaseLLM` 边界；20 个课程源码样本对比；Usage/Token/Cost 记录 | 前端不能选择模型或发起双模型对比；若按“模型软件是目标”解释，则缺少两个真实开源项目及其利用验证；现有对比主要证明模型检测能力，没有单独证明每种模型完成了受控利用验证 | 解释 A 下基本完成分析能力，但验收展示仍需补强；解释 B 下未完成 |
| 任意 2 种加壳闭源软件 | 2 个授权教育样本；严格准入清单；壳/虚拟机静态信号；统一主链；HTML/PDF 报告 | 两个样本均为 0 Finding；没有公开漏洞 Ground Truth；未动态执行；没有受控 PoC 或利用验证证据 | 部分完成，不能按完整通过申报 |
| 任意 2 种混淆闭源软件 | 2 个授权教育样本；混淆/虚拟化/JIT 保护信号；统一主链；HTML/PDF 报告 | 两个样本各只有 1 个 `UNCERTAIN` Finding；没有确认漏洞；未动态执行；没有受控 PoC 或利用验证证据 | 部分完成，不能按完整通过申报 |

## 3. 大模型测试项详细说明

### 3.1 已完成能力

- `DeepSeekAdapter`、`GLMAdapter`、`KimiAdapter` 均通过统一 LLM 抽象层访问，业务 Agent 不直接依赖厂商接口；
- 三个 Provider 已在扩展后的 20 个教学源码样本上完成真实实验；
- 每次调用能够记录 Provider、模型、输入 Token、输出 Token、总 Token、延迟和估算费用；
- 实验结果由统一 Runner 生成，能够用于比较模型在同一数据集上的表现；
- API Key 由本地 `.env` 注入，不进入报告或版本库。

规范实验摘要：`artifacts/experiments/llm-comparison/summary.md`。

### 3.2 尚未完成或容易误解的部分

1. 当前主前端没有 DeepSeek/GLM/Kimi Provider 选择器，也没有“三模型同时运行”按钮。
2. 正常审计链一次只使用配置选中的一个 Provider；三模型对比由离线实验 Runner 完成。
3. 20 个 Benchmark 是自建教学源代码样本，不是 DeepSeek、ChatGLM 或 Kimi 开源代码仓库。
4. 当前结果证明的是“模型参与漏洞候选识别”，不等价于“对模型软件自身完成漏洞利用”。
5. 模型输出不是充分证据。正式结论仍须经过 Verification，并关联非纯模型类 Evidence。

### 3.3 后续修复方案

若教师采用解释 A，建议按以下顺序补齐：

1. 在前端增加 Provider 多选区，至少允许选择两个 Provider；
2. 增加“单模型审计”和“双模型对比审计”两种模式；
3. 对同一候选分别显示发现模型、复核模型、意见是否一致、Token、费用和耗时；
4. 利用验证仍由独立 Verification/Fuzz 层执行，模型只参与计划和结果解释；
5. 增加一个可控漏洞样本，展示两个 Provider 都发现候选，验证器用无害 PoC 或崩溃输入确认，报告保留完整证据链。

若教师采用解释 B，还必须额外完成：

1. 选取两个许可证允许研究的开源大模型软件项目和固定版本；
2. 记录仓库地址、Commit、许可证、依赖和目标范围；
3. 优先选择已公开修复、可在本地复现且不会影响第三方的历史漏洞；
4. 在隔离环境中使用无害测试输入完成复现，不攻击在线服务；
5. 输出漏洞位置、调用路径、运行证据、复核结论、修复版本和前后对比。

### 3.4 完成判据

- 至少两个 Provider 可通过前端明确选择，并在同一测试任务上成功运行；
- 所有模型调用均走统一 Adapter，错误和超时能降级，不泄露密钥；
- 至少一个端到端样例同时展示发现、独立复核、受控验证和报告；
- 若按解释 B 验收，则必须再提供两个真实开源大模型项目的固定版本与复现实验。

## 4. 加壳闭源软件测试项详细说明

### 4.1 当前样本

当前严格准入清单包含两个授权教育逆向样本：

1. aj21h’s Spaghetti：UPX 与自定义 VM 保护；
2. Kowfsun custom packed crackme：自定义 C-stub 壳与字节码 VM。

样本材料因授权和仓库体积原因不进入 Git；仓库只提交清单、下载/准备说明和校验信息。当前实验采用静态分析与工具变换，`dynamic_execution=false`。

### 4.2 已完成能力

- 样本数量满足 2 个；
- 授权类型、来源、SHA-256、允许操作和执行策略可追踪；
- 能够识别 UPX、自定义壳、虚拟机与入口点等静态信号；
- 授权情况下能够执行有限的 UPX 文件变换；
- 两个样本都进入统一多 Agent 主链并生成 Evidence、HTML/PDF 报告。

### 4.3 未完成原因

- 两个样本当前均未产生 Vulnerability Finding；
- Crackme 的保护机制不等于软件漏洞；
- 成功解壳、恢复字符串或求出口令不能作为漏洞利用成功；
- 样本没有可用于 Precision/Recall 的公开漏洞 Ground Truth；
- 未执行目标，也没有 Crash、Sanitizer、Runtime Trace 或无害 PoC 证据。

### 4.4 后续修复方案

1. 保留现有两个 Crackme，继续作为“保护识别和逆向能力”演示组；
2. 另选或制作两个明确授权、具有已知漏洞 Ground Truth 的闭源教学二进制，并分别加壳；
3. 清单记录原始版本、加壳工具/参数、漏洞类型、期望触发条件和文件哈希；
4. 静态阶段不得读取 Ground Truth，Ground Truth 只由实验评分器使用；
5. 在 Windows Job Object 或等价隔离环境中执行，默认禁网、限制时间/内存/进程树；
6. 使用最小无害输入触发崩溃或安全断言，保存输入、退出码、异常、日志和复现次数；
7. 由 Verification Agent 独立确认后，才允许将状态写为 `CONFIRMED`。

### 4.5 完成判据

- 两个加壳闭源目标均有授权、固定哈希和漏洞 Ground Truth；
- 每个目标至少产生一个与 Ground Truth 对应的候选；
- 每个候选具备静态或逆向证据，以及独立动态验证证据；
- 利用验证可重复、不会联网、不会影响宿主机或第三方；
- 报告明确区分壳识别、解壳、漏洞发现和利用验证四个概念。

## 5. 混淆闭源软件测试项详细说明

### 5.1 当前样本

当前严格准入清单包含两个授权教育逆向样本：

1. CrackNotMe MCM 2.0：自定义指令虚拟化；
2. Desync C# .NET Extended Obfuscation：JIT-on-demand 保护及字符串混淆。

### 5.2 已完成能力

- 样本数量满足 2 个；
- 能够记录虚拟化、字符串混淆、JIT 保护等静态证据；
- 两个样本均进入统一多 Agent 主链；
- 每个样本产生 1 个 `UNCERTAIN` Finding，并生成可审计报告；
- 没有把不确定候选错误标记为 `CONFIRMED`。

### 5.3 未完成原因

- `UNCERTAIN` 代表证据不足，不能作为确认漏洞；
- 混淆强度或反分析机制不是漏洞本身；
- 当前目标执行次数为 0，缺少运行时或崩溃证据；
- 没有公开漏洞 Ground Truth，无法证明候选对应真实可利用缺陷。

### 5.4 后续修复方案

1. 保留现有样本作为混淆识别能力演示组；
2. 选取两个授权、具有已知漏洞的闭源教学目标，再施加可复现的混淆配置；
3. 保存混淆前后配对样本，用于比较召回率、定位误差、证据完整度和耗时；
4. 分析器不得通过文件名、目录名、样本 ID 或 Ground Truth 猜测结果；
5. 使用受控输入在隔离沙箱中复现，不生成针对公网目标的攻击载荷；
6. 对比未混淆与混淆版本，说明系统在哪些证据上退化、如何由多来源融合恢复；
7. 由 Verification Agent 基于静态和动态证据作最终状态判断。

### 5.5 完成判据

- 两个混淆闭源目标均有授权、固定哈希、混淆参数和漏洞 Ground Truth；
- 每个目标至少一个漏洞得到独立复核和受控验证；
- 报告能够展示混淆前后结果差异；
- 候选、Evidence、VerificationResult 和报告引用关系完整；
- 无 Ground Truth 泄漏和样本专用硬编码。

## 6. 当前前端的真实能力与使用方法

### 6.1 当前使用流程

1. 启动后端和前端，浏览器访问 `http://127.0.0.1:5173/`；
2. 可点击内置演示入口创建课程源码演示任务；
3. 也可在“目标源码或二进制程序相对路径”区域手动填写服务端允许目录中的相对路径；
4. 点击“浏览本地文件”可选择 C/H、Python、Go、ELF 或 PE 文件，上传完成后路径自动回填并开始分析；
5. 六阶段流水线使用统一状态：完成阶段显示勾选，未执行阶段显示未运行状态；
6. 通过 Agent 拓扑、证据链、漏洞档案、审计报告、事件遥测和 API 控制台查看结果。

### 6.2 当前不能通过前端完成的操作

- 不能在页面中选择 DeepSeek、GLM 或 Kimi；Provider 仍由后端 `.env` 的 `LLM_PROVIDER` 选择；
- 不能在一次任务中并排运行两个或三个 Provider；
- 不能从主页一键重跑完整 LLM Comparison 实验；
- 不能从主页一键重跑 4 个受保护样本的批量 Benchmark；
- 上传文件只代表允许静态分析，不自动授权动态执行；
- 页面不会为不具备 Ground Truth 的目标伪造“利用成功”结果。

### 6.3 建议新增的验收面板

后续前端建议增加“课程测试矩阵”页面，每个测试对象显示：

- 样本名称、类型、授权状态、SHA-256、Provider；
- 静态分析、逆向、Fuzz、独立复核和报告的阶段状态；
- Finding 数量及 `CONFIRMED/REJECTED/UNCERTAIN` 分布；
- 利用验证状态、复现次数、沙箱状态和 Evidence 链接；
- 模型 Token、费用、耗时和一致性；
- 明确的 `PASS/PARTIAL/FAIL/NOT_RUN` 验收状态。

验收状态必须由结构化运行数据计算，不能仅由前端颜色或静态文案声明。

## 7. 推荐开发顺序

### P0：先锁定验收口径与 Ground Truth

1. 向教师确认大模型是“分析引擎”还是“被审计目标”；
2. 为两组闭源软件确定合法授权、固定版本和已知漏洞；
3. 冻结 2 个加壳、2 个混淆样本的验收清单；
4. 为每个漏洞定义最小、无害、可重复的验证条件。

没有 Ground Truth 时继续增加规则或美化前端，无法解决“利用验证是否真实”的核心问题。

### P1：完成安全的动态验证闭环

1. 将现有 Windows Job Object 沙箱接入受保护样本 Runner；
2. 默认禁网并限制 CPU、内存、时间和子进程；
3. 记录输入、退出码、异常类型、日志、运行次数和环境；
4. 把动态证据交给独立 Verification Agent；
5. 只有证据满足策略时生成 `CONFIRMED`。

### P2：补齐前端课程测试矩阵

1. Provider 选择与双模型运行；
2. 6 个测试对象的一键运行或加载既有结果；
3. 每项验收条件的实时状态；
4. 报告、Evidence、PoC/测试输入和运行日志的一键查看；
5. API Key 仅在后端环境配置，前端不显示、不回传。

### P3：形成最终可交付实验

1. 在干净环境中重跑；
2. 固化依赖、Commit、样本哈希、模型名和时间；
3. 保存机器可读结果和 HTML/PDF 报告；
4. 执行全量 Python 测试、前端 lint/build 和密钥扫描；
5. 在 PPT 中同时展示成功项、失败项和系统边界，避免夸大。

## 8. 后续开发 Definition of Done

三项测试要求只有在以下条件全部满足后，才能将本文档结论改为“全部完成”：

- 大模型测试口径已确认，并至少完成两个对象/Provider 的可复现实验；
- 2 个加壳闭源目标均完成真实漏洞发现、独立复核和受控利用验证；
- 2 个混淆闭源目标均完成真实漏洞发现、独立复核和受控利用验证；
- 6 个目标全部具有合法授权或许可证、固定版本、哈希和 Ground Truth；
- 每个确认漏洞都关联非纯模型 Evidence；
- 所有动态验证在隔离环境运行且默认禁网；
- 前端能够如实显示每个目标的状态，不把 `UNCERTAIN` 显示为成功；
- Python 全量测试、前端 lint/build、实验复现和密钥扫描全部通过；
- `.env`、API Key、闭源样本实体、上传文件和运行时敏感数据未进入 Git。

## 9. 当前可引用的证据

- 课程原始要求：`2026网络空间安全课程设计-new.md`
- V0.4 续接状态：`VulnAgent_V0.4_开发续接说明.md`
- 高级功能规划：`VulnAgent_V0.4_高级功能开发规划.md`
- LLM 对比摘要：`artifacts/experiments/llm-comparison/summary.md`
- 受保护样本摘要：`artifacts/experiments/protected-benchmark/summary.md`
- 加壳清单：`benchmarks/packed/manifest.json`
- 混淆清单：`benchmarks/obfuscated/manifest.json`
- 前端演示说明：`演示help.md`

其中 `artifacts/` 下实验文件属于本地生成产物，是否进入最终提交由仓库发布策略决定；任何发布版本都不得包含真实 API Key 或无权再分发的闭源样本实体。

## 10. 2026-09-11 发布门禁记录

- Python 全量测试：`445 passed`；
- 前端 `npm run lint`：通过；
- 前端 `npm run build`：通过；
- `.env`：由 `.gitignore` 排除；
- `.env.example`：三个 Provider 的 Key 均为空，仅保留配置模板；
- 加壳/混淆闭源样本实体：由 `.gitignore` 排除，只提交清单与材料准备说明；
- 测试临时目录、上传文件、运行数据库、日志和生成报告：不进入源码提交。
